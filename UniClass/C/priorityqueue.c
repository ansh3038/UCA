#include <stdio.h>
#include <stdlib.h>

typedef struct{
    int val;
    int pri;
    item next;
} item;
int arr[] 
void create(int val, int pri, item* head){
    item* temp = head;
    while(temp.pri<pri)
    if(){
        
    }
}